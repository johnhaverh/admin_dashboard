import 'package:flutter/material.dart';

class AuthLayaut extends StatelessWidget {
  const AuthLayaut({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Auth Layaut'),
        )
      )
    );
  }
}